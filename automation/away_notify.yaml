 alias: No one at Home - Notification
 trigger:
   platform: state
   entity_id: group.famiglia
   state: 'not_home'
 action:
  service: notify.homenicoassistant_bot
  data:
    message: "No one is at home {{ states('sensor.date__time') }}"
